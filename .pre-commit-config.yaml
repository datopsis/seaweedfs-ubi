minimum_pre_commit_version: "4.0.0"
default_install_hook_types:
  - pre-commit
  - commit-msg

repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: 3e8a8703264a2f4a69428a0aa4dcb512790b2c8c # v6.0.0
    hooks:
      - id: check-added-large-files
        args:
          - --maxkb=1024
      - id: check-case-conflict
      - id: check-json
      - id: check-merge-conflict
      - id: check-symlinks
      # A script with a shebang that is not executable fails only when something
      # tries to run it, which is usually CI rather than the author's machine.
      - id: check-shebang-scripts-are-executable
      - id: check-yaml
      - id: destroyed-symlinks
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: fix-byte-order-marker
      - id: mixed-line-ending
        args:
          - --fix=lf
      - id: trailing-whitespace
        args:
          - --markdown-linebreak-ext=md

  - repo: https://github.com/shellcheck-py/shellcheck-py
    rev: 745eface02aef23e168a8afb6b5737818efbea95 # v0.11.0.1
    hooks:
      - id: shellcheck
        args:
          # Follow sourced files so helpers in scripts/lib are analyzed too.
          - -x

  - repo: https://github.com/shenxianpeng/hadolint-pre-commit
    rev: 6b998985ab8b766ed8104e4fcfeb0a4765f4c364 # v2.15.1.2
    hooks:
      - id: hadolint
        files: (^|/)(Containerfile|Dockerfile[^/]*)$
        args:
          - --failure-threshold
          - warning

  - repo: https://github.com/rhysd/actionlint
    rev: 914e7df21a07ef503a81201c76d2b11c789d3fca # v1.7.12
    hooks:
      - id: actionlint

  - repo: local
    hooks:
      # The lock is the only thing standing between a build and unreviewed bytes,
      # and its dangerous failure is a half-edited file that still parses. This
      # checks agreement between fields, not just shape.
      - id: check-artifact-lock
        name: validate artifact locks
        language: python
        entry: python .github/scripts/check_artifact_lock.py
        files: ^artifacts/.*\.lock\.json$

      - id: check-requirement-trace
        name: validate generated requirement trace
        language: python
        entry: python scripts/build-trace-matrix.py --check
        pass_filenames: false
        files: ^(docs/(L[123]-REQ|TRACE-MATRIX)\.md|scripts/build-trace-matrix\.py|tests/test_.*\.py)$

      - id: forbid-coauthor-trailers
        name: forbid co-author attribution trailers
        language: pygrep
        entry: "(?im)^Co-Authored-By:"
        stages:
          - commit-msg
